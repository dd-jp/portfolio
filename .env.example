# Google Analytics 4 measurement ID (free). Create a property at https://analytics.google.com,
# copy the "G-XXXXXXXXXX" ID, and set it here (and in your Netlify site's environment variables).
# Analytics only loads in production builds when this is set — dev builds never send data.
VITE_GA_ID=
